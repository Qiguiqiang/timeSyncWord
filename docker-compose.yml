version: '3.8'

services:
  opentimesync:
    build: .
    container_name: opentimesync
    restart: unless-stopped
    ports:
      - "13013:13013"
    environment:
      - PORT=13013
      - TZ=UTC
    cap_add:
      - SYS_TIME
      - NET_ADMIN
