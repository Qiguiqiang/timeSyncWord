{
  "name": "opentimesync",
  "version": "2.1.8",
  "description": "Desktop high-precision time synchronization via NTP-weighted clock (Tauri + Rust)",
  "author": "OpenTimeSync Contributors",
  "license": "MIT",
  "scripts": {
    "icons": "tauri icon build/icon.png --output src-tauri/icons",
    "dev": "npm run icons && tauri dev",
    "build": "npm run icons && tauri build",
    "tauri": "tauri"
  },
  "keywords": ["time-sync", "ntp", "synchronization", "tauri", "desktop"],
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
